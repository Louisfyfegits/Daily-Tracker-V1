{
  "name": "todo",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --format=esm",
    "watch": "esbuild index.js --bundle --outfile=dist/bundle.js --format=esm --watch"
  },
  "dependencies": {
    "firebase": "^11.7.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}